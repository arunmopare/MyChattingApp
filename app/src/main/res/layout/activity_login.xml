<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
                                                   xmlns:app="http://schemas.android.com/apk/res-auto"
                                                   xmlns:tools="http://schemas.android.com/tools"
                                                   android:layout_width="match_parent"
                                                   android:layout_height="match_parent"
                                                   android:background="@drawable/white_curvy_background"
                                                   android:backgroundTint="@color/common_google_signin_btn_text_dark_default">
    <EditText
            android:layout_height="50dp"
            android:inputType="textEmailAddress"
            android:ems="10"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent" android:id="@+id/email_edittext_login"
            android:layout_marginTop="100dp"
            app:layout_constraintTop_toTopOf="parent"
            android:layout_width="0dp"
            android:paddingLeft="20dp"
            android:layout_marginStart="32dp" android:layout_marginEnd="32dp"
            app:layout_constraintHorizontal_bias="0.0"
            android:background="@drawable/username_background"
            android:hint="Email" android:autofillHints=""/>
    <EditText
            android:layout_height="50dp"
            android:inputType="textPassword"
            android:ems="10"
            app:layout_constraintStart_toStartOf="@+id/email_edittext_login"
            app:layout_constraintEnd_toEndOf="@+id/email_edittext_login" android:id="@+id/password_edittext_login"
            app:layout_constraintTop_toBottomOf="@+id/email_edittext_login" app:layout_constraintHorizontal_bias="0.0"
            android:hint="Password"
            android:paddingLeft="20dp"
            android:background="@drawable/username_background"
            android:layout_marginTop="24dp" android:layout_width="0dp"
            android:autofillHints=""/>
    <Button
            android:text="login"
            android:layout_width="0dp"
            android:layout_height="50dp"
            app:layout_constraintStart_toStartOf="@+id/password_edittext_login"
            app:layout_constraintEnd_toEndOf="@+id/password_edittext_login"
            android:id="@+id/login_button_login"
            android:background="@drawable/background_button"
            android:layout_marginTop="76dp" app:layout_constraintTop_toBottomOf="@+id/password_edittext_login"
            app:layout_constraintHorizontal_bias="0.0"/>
    <TextView
            android:text="Back to registration"
            android:layout_width="128dp"
            android:layout_height="25dp"
            app:layout_constraintStart_toStartOf="@+id/login_button_login"
            app:layout_constraintEnd_toEndOf="@+id/login_button_login"
            android:textStyle="bold"
            android:id="@+id/back_to_registration_textview" android:layout_marginTop="28dp"
            app:layout_constraintTop_toBottomOf="@+id/login_button_login" android:textColor="#F4B400"/>

</androidx.constraintlayout.widget.ConstraintLayout>